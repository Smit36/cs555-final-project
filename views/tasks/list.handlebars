<!-- This is a basic template that generates a list of links to each task in a given list of tasks. -->

<ul class="taskList">
  {{#each task}}
    <li>
      <a class="taskLink" href="/task/{{_id}}">{{name}}</a>
    </li>
  {{/each}}
</ul>